export default function CardScreen({ cardId }: { cardId: string }) {
  return <div className="screen" style={{ paddingTop: 40, color: 'var(--ink-2)' }}>CardScreen: {cardId} (built in a later milestone)</div>
}
