export default function PackScreen({ packId }: { packId: string }) {
  return <div className="screen" style={{ paddingTop: 40, color: 'var(--ink-2)' }}>PackScreen: {packId} (built in a later milestone)</div>
}
